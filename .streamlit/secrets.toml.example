# Copy to .streamlit/secrets.toml for local use, or paste the same keys into
# Streamlit Community Cloud → app → Settings → Secrets.
#
# secrets.toml is gitignored. Never commit the real one.

# Uniware API — only needed once the "Refresh from Uniware" button is enabled.
UNIWARE_USER = "domin8@abstrabit.com"
UNIWARE_PASS = "..."

# Supabase — only needed once persistent storage is added (storage.py).
# SUPABASE_URL = "https://xxxx.supabase.co"
# SUPABASE_KEY = "..."
